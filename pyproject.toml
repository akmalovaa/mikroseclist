[project]
name = "mikroseclist"
version = "2.0.0"
description = ""
authors = [{name = "Artur Akmalov", email = "artur@akmalov.com"}]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "routeros-api>=0.18.1",
    "requests>=2.32.3",
    "pydantic-settings>=2.4.0",
    "schedule>=1.2.2",
    "loguru>=0.7.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
